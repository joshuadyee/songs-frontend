export function Header() {
  return (
    <header>
      <nav>
        <a href="/">Home</a> || <a href="/songs/index">Songs</a> || <a href="/songs/new">Add New Song</a> ||
      </nav>
    </header>
  )
}